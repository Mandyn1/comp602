using UnityEngine;

public class LocationValue : MonoBehaviour
{
    //isntance variblezs

    //for now the only values to change the location are the point increment, implementing multiplier in other user story
    public int pointsPerItem;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
}
