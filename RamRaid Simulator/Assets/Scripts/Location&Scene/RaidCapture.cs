using UnityEngine;

public class RaidCapture : MonoBehaviour
{
    //variables
    public int policeDistanceFromRaid;
    private SceneTransition sceneTransitionArea;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //assign the distance from the player
    }

    // Update is called once per frame
    void Update()
    {
        //update the distance of the police from the raid scene
    }

    void raiderCaught()
    {
        //raider is caught and the caught scene will be loaded
    }
}
